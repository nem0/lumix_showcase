import "core:engine/engine"
import "core:input"
import "core:world"
import "chess/chess" as chess
import "asteroids/asteroids" as asteroids
import "tower_defense/tower_defense" as tower_defense
import "platformer/platformer" as platformer

fn main(engine : Engine, w : World) : void {
	const input_system = input(engine);
	var name = w.getPartitionName(w.getPartitionHandle(0));
	match name {
		case "chess/chess.unv": chess.main(input_system, w);
		case "asteroids/asteroids.unv": asteroids.main(input_system, w);
		case "tower_defense/tower_defense.unv": tower_defense.main(input_system, w);
		case "platformer/platformer.unv": platformer.main(engine, w);
		case: panic("Unknown world");
	}
}
